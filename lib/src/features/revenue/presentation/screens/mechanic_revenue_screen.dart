
import 'package:flutter/material.dart';

class MechanicRevenueScreen extends StatelessWidget {
  const MechanicRevenueScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mechanic Revenue'),
      ),
    );
  }
}
