
import 'package:flutter/material.dart';

class MechanicVehicleScreen extends StatelessWidget {
  const MechanicVehicleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mechanic Vehicle'),
      ),
    );
  }
}
