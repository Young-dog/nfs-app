import 'package:flutter/material.dart';

class ControllerProfileScreen extends StatelessWidget {
  const ControllerProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Controller Profile'),
      ),
    );
  }
}
