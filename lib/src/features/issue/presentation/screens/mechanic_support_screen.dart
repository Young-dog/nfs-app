import 'package:flutter/material.dart';

class MechanicSupportScreen extends StatelessWidget {
  const MechanicSupportScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mechanic Support'),
      ),
    );
  }
}
