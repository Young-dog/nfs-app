import 'package:flutter/material.dart';

class MechanicTaskScreen extends StatelessWidget {
  const MechanicTaskScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tasks'),
      ),
    );
  }
}
